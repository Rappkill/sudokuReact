import React from 'react';

export function Header(): JSX.Element {
  return (
    <header className="header">
      <p>
        {' '}
        Life is like sudoku, if one number is misplaced then everything goes
        wrong{' '}
      </p>
    </header>
  );
}
